syntax = "proto3";//标明proto版本
 
package NetProtocol;//包名
 
//一个message相当于一个类
//1,2,3不代表参数默认值，而是参数标签
//repeated 可以理解为数组
 
message StoreRequest2 {
  string name = 1;
  int32 num = 2;
  int32 result = 3;
  repeated string myList=4;
}